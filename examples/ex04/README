# Example 4 -- Using filters in section templates

## Usage

- run `convert` to convert the files
- run `serve` to serve the current directory
- run `clean` to remove generated files

## Description

This example shows how to apply filters to meta fields and how to use the
filtered (=converted) results in section templates (see example 3 first to
understand section templates).
